# 실시간 중계 스테이지 개선 & 전경기 전광판 동적 자동갱신 대시보드 전환

## 1. 전경기 전광판 고정 문제 원인 및 해결 (신규)

### 문제 진단
- 포털 메인의 `[전경기 전광판]` 버튼(`landing.html`)을 누르면 `/mlb` 페이지(`mlb_dashboard.html`)로 이동했으나, 해당 파일이 **2026년 9월 17일 15경기 데이터로 고정(하드코딩)**되어 있어 날짜가 지나도 최신 경기로 갱신되지 않는 현상이 발생했습니다.

### 조치 내역
1. **완전 동적 실시간 대시보드로 전면 개편 (`mlb_dashboard.html`)**:
   - 하드코딩된 정적 1,994줄의 HTML을 제거하고, 서버의 실시간 경기 API(`/api/v1/matches?sport_code=BASEBALL&start_date=...`)와 연동하여 **오늘(9/20) 및 날짜별 전체 경기를 동적으로 자동 렌더링**하도록 구축했습니다.
2. **날짜 선택 칩 & 캘린더 픽커 탑재**:
   - `[🔥 오늘 9/20 (일)]`, `[9/19 (토)]`, `[9/18 (금)]`, `[9/17 (공식)]` 원터치 칩 추가.
   - 우측에 `📅 직접 날짜 선택(<input type="date">)` 기능을 추가하여 원하는 일자의 전경기 전광판을 즉시 조회 가능.
3. **리그별 필터 칩**:
   - `[전체 야구]`, `[⚾ MLB 메이저리그]`, `[🇰🇷 KBO 한국야구]`, `[🇯🇵 NPB 일본야구]` 필터를 추가하여 원하는 리그 경기만 즉시 모아볼 수 있도록 개선.
4. **실시간 30초 자동 갱신 및 바로가기 퀵 점프 바**:
   - 진행 중인 라이브 경기가 있을 때 30초 주기로 자동 새로고침(Auto-Refresh) 수행.
   - 상단 가로 스크롤 점프 바(`[🔥 밀워키 vs 볼티모어]`, `[#2 보스턴 vs 탬파베이]` 등) 클릭 시 해당 경기 카드로 부드럽게 스크롤.
5. **가로 스크롤 0% 구장 전광판 & 선발 매치업 유지**:
   - 1~9회 구장 전광판 라인스코어(R, H, E), 볼카운트 HUD(B, S, O), 좌/우완 선발투수 ERA 카드 등 기존의 높은 시인성과 완벽한 모바일 반응형 스타일을 100% 계승.
6. **중복 라우트 정리 (`app/main.py`)**:
   - `/mlb`, `/mlb-dashboard`, `/history/mlb`, `/20260917` 경로를 단일 핸들러로 통합 정리.

---

## 2. 이전 실시간 중계 스테이지 개선 내역

### ① 점수판 비틀어짐(Skew) 원천 해결
- 중앙 스테이지를 `position: absolute; left: 50%; transform: translateX(-50%); width: 240px;`로 고정하여 카드의 정확한 50% 중앙(Dead Center)에 영구 고정.
- 스코어 캡슐 내 좌측(`이닝`, 46px)과 우측(`아웃+LED`, 46px)을 완벽한 대칭으로 구성하여 스코어가 캡슐 정중앙에 위치.

### ② 경기 목록 실시간 중계창 크기 확대
- 기존 `106px`에서 **`130px`**로 창 높이를 확대하여 16:9 파노라마 뷰가 시원하게 펼쳐지고 가독성 대폭 향상.

### ③ 축구 메인(기본 배경) 8K 스타디움 비주얼 2종 신규 생성
- **MAIN TYPE A (`soccer_stadium_live.jpg`)**: 유럽 챔피언스리그 결승전급 야간 스타디움.
- **MAIN TYPE B (`soccer_stadium_arena.jpg`)**: 미래지향적 돔 아레나 센터서클 그래픽.

---

## 3. 배포 현황
- **GitHub 저장소 푸시 완료**: Commit `7ab3a37`
- **운영 사이트(`tokeon.co.kr`) 자동 배포 진행 중**
